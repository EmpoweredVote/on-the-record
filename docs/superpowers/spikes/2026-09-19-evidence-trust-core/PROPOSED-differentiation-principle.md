# PROPOSAL (pending Chris's approval) — sharpen `PRINCIPLES.md#differentiation`

Drafted 2026-09-20 from the LA-Mayor evidence-slice gold review. **Not applied.** On approval this
replaces the `## Differentiation` section of `docs/quote-curation/PRINCIPLES.md`, and the mirrored
`non-differentiating-goal` check in `audit-quotes/CHECKS.md` + the `publish-quotes` editorial
guidance are synced to match. (The evidence pipeline's judge `mechanism` gate already encodes it.)

## Why change it

The current principle makes differentiation a *preference*, and says a **contested directional goal**
is rankable "even without naming a specific mechanism, because the contest itself is the
differentiation." Chris's gold review contradicted that in practice: he **rejected** contested,
specific *targets* ("cut encampments 50% by 2028") and *vague directions* ("direct our dollars to
programs that work", "deliver immediate treatment", "work with the County"), and **kept only** quotes
naming a concrete instrument (triple housing construction; appoint a chief committed to X; create a
named Immigrant Economic Opportunity Hub; mandate acceptance of consular IDs/passports/ITINs). So the
real bar is: **a rankable stance must name the concrete, contestable policy LEVER — the means, not the
end, the number, or a gesture at action.**

## One policy choice for Chris (bracketed in the draft)

How hard is the gate?
- **(a) Hard gate** — no concrete lever ⇒ not rankable; the candidate is *absent* on that axis until a
  lever-bearing quote is found. Strictest; matches your rejections literally, but a blanket gate would
  mark many real, plainly-worded positions absent and shrink coverage.
- **(b) Default + flag (recommended)** — a lever-bearing quote is the rankable default; a goal/target/
  vague-direction quote is *flagged* for human judgment and surfaced only if the curator affirms it
  carries a real distinguishing position. Preserves coverage under human oversight; mirrors how the
  evidence pipeline already behaves (it flags, it does not silently drop).

## Proposed replacement text for `## Differentiation`

> ## Differentiation
>
> Among quotes that already pass the responsiveness gate, a rankable stance is the one that shows
> **how** the candidate would actually pursue a goal — the concrete, contestable **policy lever**: the
> specific instrument they would use. Build shelters; enforce the encampment ordinance; expand
> treatment and services; appoint a chief committed to a stated aim; create a named program; mandate
> acceptance of a specific document; triple housing construction — each names a *means* a plausible
> opponent could choose differently, and it is that choice a citizen is ranking. "Who wouldn't want
> safe, affordable housing?" gives a voter nothing to distinguish one candidate from another — and
> neither does the same goal restated as a number. Differentiation is what makes a ranking a comparison
> of *approaches* rather than a popularity contest over shared values everyone already holds.
>
> Three things resemble a mechanism but are not, and do not on their own make a quote rankable:
> - a **goal** — the agreeable end-state ("reduce homelessness", "make housing affordable") essentially
>   every candidate shares;
> - a **target or metric** — a number or deadline bolted onto the goal ("cut encampments 50% by 2028");
>   it quantifies the end but still names no means;
> - a **vague direction** — a gesture at action with no instrument ("direct our dollars to programs
>   that work", "deliver immediate treatment", "work with the county", "put an end to the
>   finger-pointing").
>
> The defect this names (`non-differentiating-goal`) is therefore simply: **the quote names no concrete
> policy lever.** A quote that offers only a goal, a target, or a vague direction fails it — being
> *contested* or *specific about the outcome* does not rescue it, because the contest a ranking needs
> is a contest over the *means*, and none is on offer. [STRENGTH — Chris to set: **(a)** such a quote
> is not rankable and the candidate is absent on that axis until a lever-bearing quote is found; **or
> (b, recommended)** such a quote is flagged for human judgment and surfaced only if a curator affirms
> it carries a real, distinguishing position — the safer choice where a blanket gate would erase
> legitimate but plainly-worded stances and shrink coverage.]
>
> A corollary for selection and extraction: the lever usually sits in the sentence *next to* the goal,
> so keep them together. "We must build much more housing. That includes housing at all income levels —
> deed-restricted affordable, market-rate, social housing, and shelters." states the mechanism in the
> second sentence; a quote that keeps only the first has thrown the differentiation away. Prefer the
> coherent two-to-three-sentence passage that carries the goal *and* its lever over the atomized goal
> alone.

## If approved — files to sync

1. `docs/quote-curation/PRINCIPLES.md` — replace `## Differentiation` with the above (strength choice resolved).
2. `.claude/skills/audit-quotes/CHECKS.md` — update the `non-differentiating-goal` judgment check to key
   on "names no concrete policy lever" (drop the old "requires both — uncontested goal AND no mechanism").
3. `.claude/skills/publish-quotes/` editorial guidance — mirror the lever/goal/target/vague-direction
   distinction + the keep-the-mechanism-sentence corollary.
